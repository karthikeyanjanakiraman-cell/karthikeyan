trading_engine:
  macro_window_min: 5
  historical_traversal_lookback: "365"
  live_lookback_days: 30
  correlation:
    initial_trigger_threshold: 0.8  # Strict 75% structural visual correlation required
