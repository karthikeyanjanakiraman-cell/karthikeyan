name: Backtest and Generate Reports

on:
  workflow_dispatch:
    inputs:
      start_date:
        description: 'Backtest Start Date (YYYY-MM-DD)'
        required: true
        default: '2025-01-01'
        type: string
      end_date:
        description: 'Backtest End Date (YYYY-MM-DD)'
        required: true
        default: '2026-01-01'
        type: string

permissions:
  contents: write

jobs:
  run-backtest:
    runs-on: ubuntu-latest
    
    env:
      INTERVAL: ${{ vars.DEFAULT_INTERVAL || '1d' }}
      LOOKBACK_WINDOW: ${{ vars.DEFAULT_LOOKBACK || '252' }}
      ASSET_UNIVERSE: ${{ vars.DEFAULT_UNIVERSE || 'sp500' }}
      GENERATE_HISTORICAL_CSV: 'true'
      UPSTOX_ACCESS_TOKEN: ${{ secrets.UPSTOX_ACCESS_TOKEN }}

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pandas numpy yfinance scikit-learn

      - name: Debug Secret Mapping
        run: |
          if [ -z "$UPSTOX_ACCESS_TOKEN" ]; then
            echo "❌ GitHub is passing an EMPTY string. The secret is missing in Repo Settings."
            exit 1
          else
            echo "✅ GitHub successfully loaded the secret into the environment."
          fi

      - name: Run Backtest Script
        run: |
          mkdir -p output
          python Indces.py \
            --start-date "${{ inputs.start_date }}" \
            --end-date "${{ inputs.end_date }}" \
            --interval "$INTERVAL" \
            --lookback "$LOOKBACK_WINDOW" \
            --universe "$ASSET_UNIVERSE"

      - name: Commit and Push Updated CSVs
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --staged --quiet || (git commit -m "Auto-update historical CSV files and reports" && git push origin HEAD)

      - name: Upload Artifacts
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: backtest-reports-and-csv
          path: |
            output/
            *.csv
          if-no-files-found: ignore
          
